# ─── Required ───────────────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# ─── Provider selection: "openai" or "anthropic" ────────────────────────────
LLM_PROVIDER=openai

# ─── OpenAI (required when LLM_PROVIDER=openai) ─────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# ─── Anthropic (required when LLM_PROVIDER=anthropic) ───────────────────────
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-5-haiku-latest

# ─── Optional ────────────────────────────────────────────────────────────────
# Number of messages kept in per-chat memory (default: 10)
MEMORY_SIZE=10

# ─── Research agent (all optional) ───────────────────────────────────────────
# Model used for /research summarisation — defaults to the main chat model.
# Set to a cheaper model to save cost, e.g. gpt-4o-mini while chat uses gpt-4o.
RESEARCH_MODEL=

# Number of web sources fetched per /research call (default mode, default: 5)
RESEARCH_RESULTS=5

# Max characters extracted per fetched page (default: 1200)
RESEARCH_SNIPPET_CHARS=1200

# How long (seconds) to cache search results before re-querying (default: 180)
SEARCH_CACHE_TTL=180

# ─── Long-term memory ─────────────────────────────────────────────────────────
# Path to the SQLite database file for persistent long-term memory (default: memory.db)
MEMORY_DB_PATH=memory.db

# ─── Basenotes API ────────────────────────────────────────────────────────────
# Base URL for your Basenotes instance
BASENOTES_BASE_URL=https://notes.dravek.com

# HTTP timeout (seconds) for Basenotes API calls
BASENOTES_TIMEOUT=10
